{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "description": "Backend Playbook Polar",
  "license": "ISC",
  "author": "",
  "keywords": [],
  "scripts": {
    "dev": "node server.js",
    "postinstall": "prisma generate",
    "migrate:deploy": "prisma migrate deploy",
    "start": "node server.js",
    "render-start": "npm run migrate:deploy && node server.js",
    "build": "echo \"nothing to build\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-session": "^1.18.1",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "prisma": "^6.13.0"
  },
  "engines": {
    "node": ">=18"
  }
}
